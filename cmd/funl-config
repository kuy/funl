#!/usr/bin/env bash

shift
if [[ 0 -eq $# ]]; then
  exit 1
fi

rc_path="$HOME/.funlrc"
line=$(cat "$rc_path" | grep -E "^$1\\.src:" | head -n 1)
if [[ -z $line ]]; then
  echo "funl: placeholder '$1' is not defined" >&2
  exit 1
fi

line=$(echo "$line" | sed -e "s/$1.src:[ ]*//")

echo "$line"
